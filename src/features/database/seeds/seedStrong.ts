// src/features/database/seeds/seedStrong.ts
// NO-OP: strong agora vive em koine_core.db (coreDb, pré-populado).
// O app usa getCoreDB() para acessar strong.

export const seedStrong = async () => {
  console.log('[Seed] seedStrong: NO-OP (strong agora em koine_core.db)');
};
